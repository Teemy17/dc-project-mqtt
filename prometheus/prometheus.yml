global:
    scrape_interval: 2s # Scrape data every 2 seconds for fast updates

scrape_configs:
    - job_name: "fish_haven_pond"
      static_configs:
          # host.docker.internal allows the Docker container to reach your host machine's port 8000
          - targets: ["host.docker.internal:8000"]
